extends ../layouts/main-layout.pug
block content 
  main.centered
    h1 #{prod.title}
    hr
    .image
      img(src=prod.url alt=prod.title) 
    h2 #{prod.price}
    span #{prod.author}
    span #{prod.categroy}
    span #{prod.isbn}
    p #{prod.description}
    include ../includes/add-favorite.pug